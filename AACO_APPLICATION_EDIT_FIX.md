# اصلاح قابلیت ویرایش درخواست AACo

## مشکل
بعد از ثبت درخواست AACo، کاربر می‌توانست دوباره درخواست جدید ثبت کند. این رفتار اشتباه بود.

## راه‌حل پیاده‌سازی شده

### 1. Frontend (AACOApplicationForm.tsx)

#### تغییرات اعمال شده:
- ✅ اضافه کردن loading state برای بارگذاری درخواست موجود
- ✅ نمایش وضعیت فعلی درخواست (در انتظار بررسی/تایید شده/رد شده)
- ✅ نمایش هشدار برای درخواست‌های تایید شده
- ✅ تغییر عنوان صفحه به "ویرایش درخواست" در حالت ویرایش
- ✅ تغییر دکمه submit به "به‌روزرسانی درخواست" در حالت ویرایش
- ✅ استفاده از PUT request برای به‌روزرسانی درخواست موجود

#### تغییرات بصری در حالت ویرایش:
- 🎨 تغییر رنگ پس‌زمینه از آبی به نارنجی/زرد (amber/orange)
- 🎨 نمایش badge "حالت ویرایش" در بالای صفحه
- 🎨 تغییر رنگ عنوان‌ها و متن‌ها به تم نارنجی
- 🎨 اضافه کردن emoji ✏️ به عنوان صفحه
- 🎨 تغییر رنگ Progress Steps به تم نارنجی
- 🎨 تغییر رنگ border کارت‌ها به نارنجی
- 🎨 نمایش برچسب "در حال ویرایش" در header کارت فرم
- 🎨 تغییر رنگ دکمه‌های navigation به نارنجی
- 🎨 هشدار بهبود یافته با طراحی بهتر برای درخواست‌های تایید شده

#### منطق کار:
```typescript
// در handleSubmit:
if (isEditMode && existingApplication) {
  // Update existing application
  response = await api.put(`/aaco-applications/${existingApplication._id}`, formData);
} else {
  // Create new application
  response = await api.post('/aaco-applications/submit', formData);
}
```

### 2. Backend (Event Service)

#### Controller جدید (updateApplication):
- ✅ بررسی مالکیت درخواست (فقط صاحب درخواست می‌تواند آن را ویرایش کند)
- ✅ اگر درخواست تایید شده باشد، بعد از ویرایش به حالت "submitted" برمی‌گردد
- ✅ Validation کامل داده‌ها
- ✅ لاگ‌گذاری مناسب

#### Route جدید:
```typescript
router.put('/:id', updateApplication);
```

### 3. رفتار سیستم

#### حالت اول: کاربر درخواست ندارد
- فرم خالی نمایش داده می‌شود
- دکمه "ثبت درخواست" نمایش داده می‌شود
- با کلیک، درخواست جدید ایجاد می‌شود

#### حالت دوم: کاربر درخواست دارد (submitted)
- فرم با داده‌های موجود پر می‌شود
- نشان‌دهنده وضعیت "در انتظار بررسی" نمایش داده می‌شود
- دکمه "به‌روزرسانی درخواست" نمایش داده می‌شود
- با کلیک، درخواست موجود به‌روزرسانی می‌شود

#### حالت سوم: کاربر درخواست تایید شده دارد (approved)
- فرم با داده‌های موجود پر می‌شود
- نشان‌دهنده وضعیت "تایید شده" نمایش داده می‌شود
- ⚠️ هشدار نمایش داده می‌شود: "درخواست شما قبلاً تایید شده است. در صورت ویرایش، مجدداً نیاز به تایید خواهد داشت."
- دکمه "به‌روزرسانی درخواست" نمایش داده می‌شود
- با کلیک، درخواست به‌روزرسانی می‌شود و وضعیت به "submitted" تغییر می‌کند

### 4. امنیت

- ✅ هر کاربر فقط می‌تواند درخواست خودش را ببیند و ویرایش کند
- ✅ بررسی مالکیت در backend
- ✅ Authentication middleware فعال
- ✅ Validation کامل داده‌ها

## فایل‌های تغییر یافته

1. `project1/frontend/src/pages/pending/AACOApplicationForm.tsx`
2. `project1/services/event-service/src/controllers/aacoApplicationController.ts`
3. `project1/services/event-service/src/routes/aacoApplicationRoutes.ts`

## تست

برای تست:
1. وارد سیستم شوید
2. به صفحه `/pending/application-form` بروید
3. فرم را پر کنید و ثبت کنید
4. دوباره به همان صفحه بروید
5. باید فرم با داده‌های قبلی پر شده باشد
6. تغییراتی ایجاد کنید و "به‌روزرسانی درخواست" را بزنید
7. تغییرات باید ذخیره شوند

## نتیجه

✅ هر کاربر فقط یک بار می‌تواند درخواست ثبت کند
✅ بعد از ثبت، می‌تواند درخواست موجود را ببیند و ویرایش کند
✅ اگر درخواست تایید شده باشد و کاربر آن را ویرایش کند، دوباره به حالت "در انتظار بررسی" برمی‌گردد
✅ هشدارهای مناسب به کاربر نمایش داده می‌شود
